services:
  - type: web
    name: ai-wave-trader
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:server --workers 1 --threads 8 --timeout 120 -b 0.0.0.0:$PORT"
