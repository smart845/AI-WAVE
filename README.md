# 🚀 AI Wave Trader Pro (Dash Edition)

FastAPI + Plotly Dash приложение, совместимое с Vercel.

## ⚙️ Локальный запуск
```bash
pip install -r requirements.txt
python main.py
```
Откроется по адресу: http://127.0.0.1:8000

## 🌐 Деплой на Vercel
1. Создай новый репозиторий на GitHub.
2. Добавь эти файлы (`main.py`, `requirements.txt`, `vercel.json`).
3. Подключи к https://vercel.com → New Project → Import Git Repository.
4. Деплой произойдёт автоматически.

✅ Совместимо с Vercel (Serverless Function, <50MB).
